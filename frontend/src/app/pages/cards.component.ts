import { Component, inject } from '@angular/core';
import { ApiService, Card } from '../core/api.service';

@Component({ selector: 'fo-cards', standalone: true, template: `<section class="page-shell"><div class="hero"><div><p class="eyebrow">COLECCIÓN</p><h1>La tienda de cartas</h1><p class="muted">Descubre nuevas formas de avanzar.</p></div><span class="hero-chip">{{ cards.length }} cartas activas</span></div>@if (message) { <div class="notice">{{ message }}</div> }<div class="card-grid">@for (card of cards; track card.id) { <article class="game-card"><div class="card-image">@if (card.image_url) { <img [src]="card.image_url" [alt]="card.titulo" loading="lazy"> } @else { <span>✦</span> }</div><div class="card-content"><div class="card-top"><span class="card-tag">CARTA</span><span>{{ card.quantity }} en colección</span></div><h2>{{ card.titulo }}</h2><p>{{ card.descripcion || 'Una nueva aventura te espera.' }}</p><div class="card-footer"><strong>{{ card.precio }} ⚡</strong><button class="small-button" (click)="buy(card)" [disabled]="busy === card.id">{{ busy === card.id ? 'Comprando…' : 'Conseguir' }}</button></div></div></article> } @empty { <div class="empty-state">No hay cartas disponibles.</div> }</div></section>`, styles: [] })
export class CardsComponent { private readonly api = inject(ApiService); cards: Card[] = []; message = ''; busy: number | null = null; constructor() { this.api.cards().subscribe({ next: (cards) => this.cards = cards, error: () => this.message = 'No se pudieron cargar las cartas.' }); } buy(card: Card): void { this.busy = card.id; this.message = ''; this.api.buyCard(card.id).subscribe({ next: (response) => { this.message = response.message; card.quantity += 1; this.busy = null; }, error: (err: { error?: { detail?: string } }) => { this.message = err.error?.detail ?? 'No se pudo comprar la carta.'; this.busy = null; } }); } }
