import { Component, inject } from '@angular/core';
import { FormsModule } from '@angular/forms';
import { ApiService, User } from '../core/api.service';

@Component({ selector: 'fo-students', standalone: true, imports: [FormsModule], template: `<section class="page-shell"><div class="hero"><div><p class="eyebrow">ADMINISTRACIÓN</p><h1>Alumnos</h1><p class="muted">Gestiona progreso, cursos y recompensas.</p></div><button class="primary-button" (click)="showCreate = !showCreate">+ Nuevo alumno</button></div>@if (showCreate) { <form class="surface form-grid" (ngSubmit)="create()"><input [(ngModel)]="draft.nombre" name="nombre" placeholder="Nombre" required><input [(ngModel)]="draft.email" name="email" type="email" placeholder="Email" required><input [(ngModel)]="draft.password" name="password" type="password" placeholder="Contraseña" required><input [(ngModel)]="draft.curso" name="curso" placeholder="Curso"><button class="primary-button" type="submit">Crear</button></form> }@if (message) { <div class="notice">{{ message }}</div> }<article class="surface table-wrap"><table><thead><tr><th>Nombre</th><th>Email</th><th>Curso</th><th>Nivel</th><th>XP</th><th>Electronios</th><th></th></tr></thead><tbody>@for (student of students; track student.id) { <tr><td>{{ student.nombre }}</td><td>{{ student.email }}</td><td>{{ student.curso || '—' }}</td><td>{{ student.nivel ?? 0 }}</td><td><input class="inline-input" type="number" [(ngModel)]="student.exp" (change)="save(student)"></td><td><input class="inline-input" type="number" [(ngModel)]="student.electronios" (change)="save(student)"></td><td><button class="danger-button" (click)="remove(student)">Eliminar</button></td></tr> }</tbody></table></article></section>`, styles: [] })
export class StudentsComponent { private readonly api = inject(ApiService); students: User[] = []; showCreate = false; message = ''; draft = { nombre: '', email: '', password: '', curso: '' }; constructor() { this.load(); } load(): void { this.api.students().subscribe({ next: (students) => this.students = students, error: (err: { error?: { detail?: string } }) => this.message = err.error?.detail ?? 'No se pudieron cargar los alumnos.' }); } create(): void { this.api.createStudent(this.draft).subscribe({ next: () => { this.message = 'Alumno creado.'; this.showCreate = false; this.draft = { nombre: '', email: '', password: '', curso: '' }; this.load(); }, error: (err: { error?: { detail?: string } }) => this.message = err.error?.detail ?? 'No se pudo crear.' }); } save(student: User): void { this.api.updateStudent(student.id, { exp: student.exp, electronios: student.electronios }).subscribe({ next: () => this.message = 'Cambios guardados.', error: () => this.message = 'No se pudo guardar.' }); } remove(student: User): void { if (!confirm(`¿Eliminar a ${student.nombre}?`)) return; this.api.deleteStudent(student.id).subscribe({ next: () => this.load() }); } }
